队列：
    数据：
        同类型
    关系：
        操作为数组的子集
        从队尾添加元素，从队首取出元素


    数组队列与循环队列

    数组队列：
            复杂度分析：
                   出队为O(n)

    循环队列：
            均摊复杂度分析：
                    均为O(1)