栈：
    线性数据结构

    数据：
        同种类型的数据

    关系：
        只能从一端添加数据，另外一端取出数据（栈顶）
        （操作为数组的子集）
        先进后出,后进先出（LIFO）

        常见操作：
            void push()
            E pop()
            E peek()
            int getSize()
            boolean isEmpty()

        分析：
            用户：支持操作就好
                  但是底层实现又多种方式

    应用：
        IDE的Undo操作（撤销），用栈管理

        程序调用使用的系统栈，用栈记录函数的调用过程

        括号匹配