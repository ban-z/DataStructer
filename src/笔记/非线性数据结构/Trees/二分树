二叉树：
    动态数据结构
    具有唯一根节点
    同样具有天然的递归结构


二分搜索树；
    二叉树的子类
    每个节点的值：
                大于左子树所有节点的值
                小于右子树所有节点的值
     每一颗子树也是二分搜索树

     查找数据更加高效

     条件：存储的元素需要具有可比较性

     特性：二分搜索树的顺序性（中序遍历）
           最小值，最大值，给定值的前驱后继的操作