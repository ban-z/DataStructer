集合Set：每种元素只能存在一次（去重）
        BST与LinkedList  均可实现set

        复杂度分析(平均)：

                            BSTSet                          LinkedListSet
        增add                 h(log n)                             N                 h:树高度       2^h - 1 = N  /  h = O(log2 N)

        查contains            h                                    N

        删remove              h                                    N
                        最差：O（n）：退化为列表


        有序集合（使用二叉搜索树：元素存储具有顺序性）和无序集合（使用列表：元素存储没有顺序性）

        基于哈希表实现链表集合会极大提高效率

        多重集合：元素可以重复
                    可以用有重复二叉搜索树实现